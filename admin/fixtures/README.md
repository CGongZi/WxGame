# Fixtures

| File | Expect |
|------|--------|
| `invalid-minimal.json` | `npm run validate` exits **1** |
| `seed-draft.json` | `npm run validate:seed` exits **0** (small CRUD seed) |
| `builtin-pack.json` | Generated by `npm run export:builtin` (gitignored) — full pack for K14 |

K14: `npm start` then `npm run smoke:k14` publishes `sword.damage=35` to `/api/pack/latest`.  
Client: `ConfigRemote.configure({ url: 'http://127.0.0.1:8787/api/pack/latest', lockRemote: false })` then cold boot.
